%h1 Listing donors

%table
  %tr
    %th Email
    %th Name
    %th Venmo
    %th
    %th
    %th

  - @donors.each do |donor|
    %tr
      %td= donor.email
      %td= donor.name
      %td= donor.venmo_id
      %td= link_to 'Show', donor
      %td= link_to 'Edit', edit_donor_path(donor)
      %td= link_to 'Destroy', donor, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Donor', new_donor_path
